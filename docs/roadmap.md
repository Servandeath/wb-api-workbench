# Roadmap

## v0.1 — Базовая структура (готово)

- [x] структура проекта;
- [x] Git;
- [x] README;
- [x] виртуальное окружение;
- [x] pyproject.toml.

## v0.2 — Безопасность и доступ (готово)

- [x] хранение ключей через keyring;
- [x] хранение ключей в зашифрованном файле;
- [x] временное хранение ключа в сессии;
- [x] роли доступа;
- [x] проверка прав.

## v0.3 — GUI MVP (готово)

- [x] запуск GUI;
- [x] выбор роли и режима (Settings);
- [x] диагностика окружения (Diagnostics);
- [x] раздел Users: создание, смена роли, деактивация;
- [x] JSON-персистентность пользователей;
- [x] скролл в списке пользователей;
- [x] менеджер ключей (Keys GUI, WB + Ozon);
- [x] поле временного API-ключа (API Tester, Test mode).

## v0.4 — API и данные (в работе)

- [x] подключение SQLite / SQLAlchemy;
- [x] история запросов в БД;
- [x] раздел API Tester (GET/POST, выбор раздела WB API — у каждого
      свой хост, Test/Real режимы, опциональное сохранение ответа);
- [ ] импорт локальных выгрузок (JSON / CSV / Excel).

## v1.0 — Портфолио-версия

- [ ] документация;
- [ ] скриншоты (актуализировать под API Tester и текущий Keys/Users);
- [x] стабильный GUI (main_window.py разобран на секции-миксины,
      каждый раздел — свой файл в app/gui/sections/);
- [x] тесты (127, core-логика; GUI по-прежнему без юнит-тестов —
      только ручная проверка, реального Tk в CI нет);
- [ ] пример работы приложения.
